name: DeepSeek ULTRA Enterprise Architect
on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write

jobs:
  ultra_architect:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]' && (github.event_name == 'issues' || github.event_name == 'issue_comment')
    steps:
      - name: Ultra Güvenli Depo Checkout
        uses: actions/checkout@v4

      - name: DeepSeek ULTRA Architect Bot Çalıştır
        uses: actions/github-script@v7
        env:
          API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        with:
          script: |
            /**
             * ╔═══════════════════════════════════════════════════════════════╗
             * ║       ULTRA ENTERPRISE ARCHITECT BOT v2.2 (YAML DÜZELTİLMİŞ)  ║
             * ║       Powered by DeepSeek AI                                  ║
             * ╚═══════════════════════════════════════════════════════════════╝
             * 
             * DÜZELTMELER:
             * ✓ YAML uyumlu çok satırlı script formatı
             * ✓ Doğru girinti ve escape karakterleri
             * ✓ API URL'si ve hata yönetimi iyileştirildi
             */

            console.log("🚀 [Ultra Bot] İşlem başlatılıyor...");
            console.log(`📍 [Ultra Bot] Event: ${context.eventName}`);
            
            const userMessage = context.payload.comment ? context.payload.comment.body : context.payload.issue.body;
            const apiKey = process.env.API_KEY ? process.env.API_KEY.trim() : "";
            
            function throwCritical(msg) {
              console.error(`‼️ [Ultra Bot Kritik Hata]: ${msg}`);
              throw new Error("‼️ [Ultra Bot Kritik]: " + msg);
            }
            
            if (!apiKey) throwCritical("API anahtarı yüklü değil! DEEPSEEK_API_KEY secret'ını ekleyin.");
            if (!userMessage || userMessage.trim().length === 0) throwCritical("Kullanıcı mesajı boş!");
            
            console.log(`✅ [Ultra Bot] Validasyonlar başarılı. Mesaj uzunluğu: ${userMessage.length} karakter`);

            const systemPrompt = `
Sen 1 numaralı global yazılım (ULTRA ENTERPRISE) mimarısın.
Her geliştirme sorumluluğun; kod, mimari, güvenlik, sürdürülebilirlik, ölçeklenebilirlik, ultra temizlik ve %100 işlevdir.

++ KURUMSAL KURALLAR ++
- Daima modüler ve en iyi klasör yapısını kur.
- Kod içine anlamlı, açıklayıcı, ENGLISH ve TURKISH yorum ekle. (Kodun başında ve önemli bloklarda)
- Kod, dökümantasyon, test, ortam dosyası (ör: .env.example), gereksiz bırakılamaz.
- Her dosya sadece ilgi alanını kapsayacak.
- En az: src/, tests/, docs/, configs/, .env.example, readme.md çıkmalı, dil uygunsa dockerfile ve CI/Cd örneği olmalı.
- Dosya silinmesi istenirse, kullanıcı "sil: ..." (sil/comma/space) ile belirtir. Ultra güvenli sil.
- Hiçbir şekilde sohbet ya da insan gibi mesajlaşma, ek format, yan not kullanılamaz.
- Her dosya ve silme işlemi ÖZEL formatta olmalı.

++ DOSYA EKLE-GÜNCELLE BLOK FORMAT ++
---FILENAME: path/name.ext---
KODUN TAMAMI (gerekli açıklamalarla)
---END_OF_FILE---

++ DOSYA SİLME BLOK FORMAT ++
---DELETE: path/filename.ext---
---END_DELETE---

++ ÖRNEK ++
---FILENAME: src/main.py---
# [EN] Main entrypoint | [TR] Ana giriş noktası
print("App started (Başladı)")
---END_OF_FILE---
---DELETE: src/old.py---
---END_DELETE---

Print only file/silme bloklarını üret, sohbet veya öneri eklenemez.
            `.trim();

            console.log("🟢 [Ultra Bot] DeepSeek API çağrısı başlatılıyor...");
            const apiUrl = "https://api.deepseek.com/v1/chat/completions";
            console.log(`🌐 [Ultra Bot] Endpoint: ${apiUrl}`);
            
            try {
              const response = await fetch(apiUrl, {
                method: "POST",
                headers: {
                  "Authorization": `Bearer ${apiKey}`,
                  "Content-Type": "application/json"
                },
                body: JSON.stringify({
                  model: "deepseek-chat",
                  messages: [
                    { role: "system", content: systemPrompt },
                    { role: "user", content: "PROJE TALEBİ: " + userMessage }
                  ],
                  temperature: 0.15,
                  stream: false,
                  max_tokens: 4000
                })
              });
              
              console.log(`📡 [Ultra Bot] HTTP Status: ${response.status} ${response.statusText}`);
              
              if (!response.ok) {
                const errText = await response.text();
                console.error(`❌ [Ultra Bot] API Error: ${response.status} - ${errText}`);
                throwCritical(`DeepSeek API hatası (${response.status}): ${errText}`);
              }
              
              const data = await response.json();
              console.log(`✅ [Ultra Bot] API yanıtı alındı`);
              
              if (!data.choices || !data.choices[0] || !data.choices[0].message) {
                throwCritical("API yanıtı beklenmeyen formatta");
              }
              
              const aiReply = data.choices[0].message.content;
              console.log(`📝 [Ultra Bot] AI yanıt boyutu: ${aiReply.length} karakter`);
              console.log(`🤖 [Ultra Bot] AI yanıtı: ${aiReply.substring(0, 200)}...`);

              console.log("📁 [Ultra Bot] Dosya bloklarını işlemeye başlıyor...");
              const fileRE = /---FILENAME:\s*(.*?)---\n([\s\S]*?)---END_OF_FILE---/g;
              let match, filesDone=[], filesProblems=[];
              let resultLog = "### 🟢 Ultra Enterprise Sonuç Raporu\n\n";
              resultLog += `**📅 Tarih:** ${new Date().toISOString()}\n`;
              resultLog += `**👤 İstek:** @${context.actor}\n\n`;
              
              let fileBlockFound = false;
              while ((match = fileRE.exec(aiReply)) !== null) {
                fileBlockFound = true;
                const fname = match[1].trim();
                const fcont = match[2].trim();
                
                console.log(`📝 [Ultra Bot] İşleniyor: ${fname} (${fcont.length} bytes)`);
                
                if (!fname || fname.includes('..') || fname.startsWith('/')) {
                  filesProblems.push({fname, err: 'Geçersiz dosya adı'});
                  resultLog += `\n❌ \`${fname}\` - Geçersiz dosya adı`;
                  continue;
                }
                
                let sha = undefined;
                try {
                  const {  existing } = await github.rest.repos.getContent({
                    owner: context.repo.owner, repo: context.repo.repo, path: fname
                  });
                  sha = existing.sha;
                } catch(_) {/* dosya yoksa */}
                
                try {
                  await github.rest.repos.createOrUpdateFileContents({
                    owner: context.repo.owner, repo: context.repo.repo, path: fname,
                    message: `[ULTRA BOT]: ${fname} ${sha ? 'güncellendi' : 'oluşturuldu'}`,
                    content: Buffer.from(fcont).toString('base64'), sha
                  });
                  filesDone.push(fname);
                  resultLog += `\n✅ \`${fname}\` ${sha ? 'güncellendi' : 'oluşturuldu'}`;
                  console.log(`✅ [Ultra Bot] Başarılı: ${fname}`);
                } catch (err) {
                  filesProblems.push({fname, err: err.message});
                  resultLog += `\n❌ \`${fname}\`: ${err.message}`;
                  console.error(`❌ [Ultra Bot] Hata ${fname}: ${err.message}`);
                }
              }
              console.log(`📁 [Ultra Bot] Dosya işleme tamamlandı: ${filesDone.length} başarılı`);

              console.log("🗑️ [Ultra Bot] Silme bloklarını işlemeye başlıyor...");
              const delRE = /---DELETE:\s*(.*?)---\s*---END_DELETE---/g;
              let delDone=[], delFail=[];
              let delMatch;
              let deleteBlockFound = false;
              
              while ((delMatch = delRE.exec(aiReply)) !== null) {
                deleteBlockFound = true;
                const fname = delMatch[1].trim();
                
                console.log(`🗑️ [Ultra Bot] Siliniyor: ${fname}`);
                
                if (!fname || fname.includes('..') || fname.startsWith('/')) {
                  delFail.push({fname, err: 'Geçersiz dosya adı'});
                  resultLog += `\n⚠️ \`${fname}\` - Geçersiz dosya adı`;
                  continue;
                }
                
                const protectedFiles = [
                  '.gitignore', 
                  'README.md', 
                  '.github/workflows/deepseek.yml',
                  'package.json',
                  'package-lock.json',
                  'yarn.lock',
                  'requirements.txt',
                  'Gemfile',
                  'Gemfile.lock',
                  'go.mod',
                  'go.sum',
                  'Cargo.toml',
                  'Cargo.lock'
                ];
                if (protectedFiles.includes(fname)) {
                  delFail.push({fname, err: 'Korumalı dosya'});
                  resultLog += `\n🛡️ \`${fname}\` - Korumalı dosya`;
                  console.warn(`🛡️ [Ultra Bot] Korumalı: ${fname}`);
                  continue;
                }
                
                try {
                  const {  existing } = await github.rest.repos.getContent({
                    owner: context.repo.owner, repo: context.repo.repo, path: fname
                  });
                  await github.rest.repos.deleteFile({
                    owner: context.repo.owner, repo: context.repo.repo, path: fname,
                    message: `[ULTRA BOT]: ${fname} silindi`,
                    sha: existing.sha
                  });
                  delDone.push(fname);
                  resultLog += `\n🗑️ \`${fname}\` silindi`;
                  console.log(`✅ [Ultra Bot] Silindi: ${fname}`);
                } catch(err) {
                  delFail.push({fname, err: err.message});
                  resultLog += `\n⚠️ \`${fname}\`: ${err.message}`;
                  console.error(`❌ [Ultra Bot] Silme hatası: ${err.message}`);
                }
              }
              console.log(`🗑️ [Ultra Bot] Silme tamamlandı: ${delDone.length} başarılı`);

              const totalOps = filesDone.length + delDone.length;
              const totalErrors = filesProblems.length + delFail.length;
              
              resultLog += `\n\n---\n### 📊 İşlem Özeti\n`;
              resultLog += `- ✅ **Başarılı:** ${totalOps} işlem\n`;
              resultLog += `- ❌ **Hatalı:** ${totalErrors} işlem\n`;
              resultLog += `- 📁 **Oluşturulan/Güncellenen:** ${filesDone.length} dosya\n`;
              resultLog += `- 🗑️ **Silinen:** ${delDone.length} dosya\n`;
              
              if (filesProblems.length > 0) {
                resultLog += `\n### ⚠️ Oluşturulamayan Dosyalar\n`;
                for (const fp of filesProblems) resultLog += `- \`${fp.fname}\`: ${fp.err}\n`;
              }
              if (delFail.length > 0) {
                resultLog += `\n### ⚠️ Silinemeyen Dosyalar\n`;
                for (const df of delFail) resultLog += `- \`${df.fname}\`: ${df.err}\n`;
              }
              if (!fileBlockFound && !deleteBlockFound) {
                resultLog += `\n---\n### ℹ️ Bilgi\n`;
                resultLog += `AI yanıtında dosya/silme bloğu tespit edilmedi.\n\n`;
                const maxPreviewLength = 500;
                const sanitizedReply = aiReply.substring(0, maxPreviewLength).replace(/[<>]/g, '');
                resultLog += `<details><summary>AI Yanıt Önizlemesi (ilk ${maxPreviewLength} karakter)</summary>\n\n\`\`\`\n${sanitizedReply}${aiReply.length > maxPreviewLength ? '\n...(kısaltıldı)' : ''}\n\`\`\`\n</details>`;
              }
              
              resultLog += `\n\n---\n*🤖 DeepSeek ULTRA Enterprise Architect v2.2 | YAML Uyumlu*`;
              
              console.log(`📤 [Ultra Bot] Rapor gönderiliyor...`);
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: context.issue.number,
                body: resultLog
              });

              console.log(`\n${'='.repeat(50)}`);
              console.log(`🎉 [ULTRA BOT] İşlem Tamamlandı!`);
              console.log(`📊 Toplam: ${totalOps} başarılı, ${totalErrors} hatalı`);
              console.log(`✨ Tüm talepler profesyonel kaliteyle karşılandı.`);
              console.log(`${'='.repeat(50)}\n`);
              
            } catch (error) {
              console.error(`💥 [Ultra Bot] Beklenmeyen Hata: ${error.message}`);
              console.error(error.stack);
              
              const errorLog = `### ❌ Kritik Hata Raporu\n\n`;
              errorLog += `**Tarih:** ${new Date().toISOString()}\n`;
              errorLog += `**Hata:** ${error.message}\n`;
              errorLog += `\`\`\`\n${error.stack.substring(0, 1000)}\n\`\`\`\n\n`;
              errorLog += `**Çözüm Önerileri:**\n`;
              errorLog += `- DEEPSEEK_API_KEY secret'ının doğru olduğundan emin olun\n`;
              errorLog += `- API limitlerini kontrol edin\n`;
              errorLog += `- İstek formatını kontrol edin\n\n`;
              errorLog += `*🤖 DeepSeek ULTRA Enterprise Architect v2.2 | Hata Yönetimi*`;
              
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: context.issue.number,
                body: errorLog
              });
              
              throw error;
            }
